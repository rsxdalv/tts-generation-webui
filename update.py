def main():
    print("Legacy installer is guaranteed to break installation, including torch+cuda")
    print("For this reason, it is disabled")
    print(
        "It is recommended to use the new installer in a new directory, and leave the existing installation intact"
    )
    print(
        "The new installer can be downloaded from https://github.com/rsxdalv/tts-generation-webui/"
    )
    print(
        "If the current installation works, you can keep using it and install the new version in parallel."
    )


if __name__ == "__main__":
    main()
